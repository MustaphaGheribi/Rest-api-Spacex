##Simple Rest API That consumes the Spacex API

###Endpoints: 
    
    /crew/space
    
returns All the crew members information that went to space

####Tests are also provided

To run the application on docker:
     
     docker-compose up -d
